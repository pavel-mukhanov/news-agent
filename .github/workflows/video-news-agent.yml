name: Video News Agent

on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  digest:
    runs-on: ubuntu-latest
    environment: news-agent
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Restore digest state cache
        uses: actions/cache@v4
        with:
          path: .news-agent
          key: video-news-state-v2-${{ github.repository }}-${{ github.ref_name }}
          restore-keys: |
            video-news-state-v2-${{ github.repository }}-

      - name: Build video news digest
        env:
          NEWS_FEEDS: ${{ vars.NEWS_FEEDS }}
          NEWS_KEYWORDS: ${{ vars.NEWS_KEYWORDS }}
          NEWS_BLOCKED_DOMAINS: ${{ vars.NEWS_BLOCKED_DOMAINS }}
          NEWS_BLOCKED_URL_PREFIXES: ${{ vars.NEWS_BLOCKED_URL_PREFIXES }}
          NEWS_MAX_ITEMS: ${{ vars.NEWS_MAX_ITEMS }}
          NEWS_MAX_AGE_DAYS: ${{ vars.NEWS_MAX_AGE_DAYS }}
          NEWS_SEEN_FILE: .news-agent/history.json
          NEWS_SEEN_LOOKBACK_DAYS: ${{ vars.NEWS_SEEN_LOOKBACK_DAYS }}
          NEWS_OUTPUT_FILE: artifacts/video-news-digest.md
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python scripts/video_news_digest.py

      - name: Publish digest as artifact
        uses: actions/upload-artifact@v4
        with:
          name: video-news-digest
          path: artifacts/video-news-digest.md
          if-no-files-found: error

      - name: Add digest to job summary
        run: |
          echo "## Video Encoding News Digest" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          cat artifacts/video-news-digest.md >> "$GITHUB_STEP_SUMMARY"
